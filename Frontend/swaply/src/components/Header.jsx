import React from 'react';
import SearchBar from './SearchBar';

const Header = () => {
    return (
        <header>
            <h1>Mi Sitio</h1>
            <SearchBar />
        </header>
    );
};

export default Header;
